eureka:
  instance:
    non-secure-port-enabled: true
    secure-port-enabled: false
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8762
spring:
  application:
    name: service-hi
